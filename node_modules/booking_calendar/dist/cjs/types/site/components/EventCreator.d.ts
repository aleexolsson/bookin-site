/// <reference types="react" />
import { EventType } from '../../interfaces';
export default function EventCreator({ addNewEvent, timezone }: {
    addNewEvent: (event: EventType) => void;
    timezone: string;
}): JSX.Element;
