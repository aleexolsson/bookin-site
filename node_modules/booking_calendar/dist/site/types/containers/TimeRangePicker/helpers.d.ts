import { Time } from '../../helpers';
/**
   * This function returns an array with the times or dashes that will be displayed in the Time Range Picker
   * The dashes represents unavailable times
   * @param {Time[]} enabledSlots an array with all the available times without the dashes
*/
export declare const getTimes: (enabledSlots: Time[]) => (string | Time)[];
